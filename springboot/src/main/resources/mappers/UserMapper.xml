<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="top.roud.cms.mapper.UserMapper">

    <select id="selectUserByPhonenumberAndPassword" resultType="User">
        select id,nickname,phonenumber,password,registertime,type,power from sv_user where phonenumber = #{phonenumber} and password = #{password}
    </select>
</mapper>